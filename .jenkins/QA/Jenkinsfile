// .jenkins/QA/Jenkinsfile — QA rerun pipeline entrypoint.
// Consumes packaged artifacts from Build/Development and reruns automation tests.

@Library('kano-jenkins-unreal-pipeline-library') _

unrealQaPipeline(
    qaWorkspacePath: "${env.WORKSPACE}"
)
