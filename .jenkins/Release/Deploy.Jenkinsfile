// .jenkins/Release/Deploy.Jenkinsfile — deploy job under the Release folder.
// Consumes artifacts from Build/Development and Build/UGSBuild.

@Library('jenkins-unreal-pipeline-library') _

unrealReleaseDeployPipeline()
