// .jenkins/Release/Jenkinsfile — compatibility wrapper for Jenkins jobs still
// configured to load the legacy release entrypoint path.
@Library('jenkins-unreal-pipeline-library') _

unrealReleaseDeployPipeline()
